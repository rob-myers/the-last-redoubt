import { QedIcon } from 'components/page/Icon'

#### Case: Both A and B are moving

If the axis-aligned rectangles below don't intersect then the NPCs won't collide.<QedIcon/>
- $\tt npcA.getWalkSegBounds()$
- $\tt npcB.getWalkSegBounds()$


Otherwise for $X \in \{ A, B \}$ we have time-varying positions:
- $p_X(t) := src_X + t . s_X . tangent_X$
- $src_X := {\tt npcX.getPosition()}$
- $dst_X := {\tt npcX.getTarget()}$
- $tangent_X := \widehat{dst_X - src_X}$

<!-- from here: time bounds? -->

Solving $a.t^2 + b.t + c ≤ 0$,
- $a := s_A^2 + s_B^2 - 2 . s_A . s_B . \hat{d}_A \cdot \hat{d}_B$
- $b := 2. (s_A . dp_A - s_B . dp_B)$
- $c := |p_B - p_A|^2 - dist_{\rm min}^2$
