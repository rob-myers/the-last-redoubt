<!-- TODO remove this old approach -->

import { QedIcon } from 'components/page/Icon'

We can rewrite the inequality as a quadratic in two variables:
- $k_0.t^2 + k_1.λ^2 + k_2.λt + k_3.t + k_4.λ + k_5 ≤ 0$
- $k_0 := u^2$
- $k_1 := 1$
- $k_2 := -2 .u.(\tau_0 · \tau_1)$
- $k_3 := -u.(\tau_0 · (a_1 - a_0))$
- $k_4 := (a_1 - a_0) · \tau_1$
- $k_5 := |a_1 - a_0|^2 - r^2$

<!-- TODO verify by hand again -->

We note that if $\tau_0 · (a_1 - a_0) \leq 0$ the NPC moves away from the line segment, so no collision.<QedIcon/>
Regardless, fixing $\lambda$ we have a quadratic in $t$ whose solutions take the form $(-b ± \sqrt{inSqrt}) / 2a$ where:
- $a := u^2$
- $b := k_2 . \lambda + k_3$
- $c := k_1 . \lambda^2 + k_4 . \lambda + k_5$
- $inSqrt := b^2 - 4ac$

Given $inSqrt < 0$  then there are no solutions and the NPC does not collide.<QedIcon/>
If $inSqrt = 0$ there is exactly one solution,
a "glancing collision", and we may assume the NPC does not collide.<QedIcon/>
Otherwise there are exactly two solutions.
We first show how to force both (a) $\tau_0 \cdot \tau_1 \geq 0$, and (b) $(a_1 - a_0) · \tau_1 \geq 0$.

<!--
IDEA can force both 𝛕0·𝛕1 > 0 and (a1 - a0)·𝛕1 > 0 🚧
 -->

<!--
- $b \leq 0$ because ...
- $4.a.c \geq 0$ can be forced.
-->

<!-- TODO conditions under which k_4 ≥ 0 (so c ≥ 0, so 4ac ≥ 0) -->

<!--
Split into cases based on sign of b?

Problematic case from above is $k_4 < 0$
i.e. NPC starts "beyond" line seg...

Under this condition what can we say about -b - √(b^2 - 4ac) ?

-->